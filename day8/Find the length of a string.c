/*
Find the length of a string.
*/
#include<stdio.h>
#include<string.h>

int main(){
    char st[]="hi hay 8 of learning dsa";
    int length = strlen(st);
    printf("the length of string:%d",length);
    return 0;
}